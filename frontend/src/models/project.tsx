import { IconInfo } from "types";

import GroupWorkIcon from "@material-ui/icons/GroupWork";

const ProjectIcon: IconInfo = {
  Icon: GroupWorkIcon,
  iconProps: { style: { color: "var(--project-color)" } },
};

export default ProjectIcon;
