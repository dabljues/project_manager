import styled from "styled-components/macro";

const PageContent = styled.div`
  // flex-grow, flex-shrink, flex-basis
  // equivalent to flex: 1 auto
  // equivalent to flex: auto
  flex: 1 1 auto;
`;

export default PageContent;
